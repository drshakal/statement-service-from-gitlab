management:
  endpoints:
    web:
      exposure:
        include: "*"
        
spring:
  cache:
    type: redis
  redis:    host: ${RHOST}
    port: ${RPORT}
    password: ${RPASSWORD}
urls: 
  account-service: ${ACCOUNT-SERVICE}
  restart-service: ${RESTART-SERVICE}